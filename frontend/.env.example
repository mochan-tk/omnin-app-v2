# Sample environment variables for frontend
# Copy this to .env.local (not committed) and adjust as needed.

# URL of the backend FastAPI service (no trailing slash)
BACKEND_URL=http://localhost:8000
